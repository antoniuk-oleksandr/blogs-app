services:
  blogs-app-db:
    container_name: blogs-app-db
    image: postgres:18.0-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - default

  blogs-app-opensearch:
    container_name: blogs-app-opensearch
    image: opensearchproject/opensearch:3.5.0
    restart: unless-stopped
    environment:
      - cluster.name=opensearch-cluster
      - node.name=opensearch-node
      - discovery.type=single-node
      - bootstrap.memory_lock=true
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
      - DISABLE_SECURITY_PLUGIN=true
    ulimits:
      memlock:
        soft: -1
        hard: -1
    ports:
      - "9200:9200"
      - "9600:9600"
    networks:
      - default

  blogs-app-rabbitmq:
    container_name: blogs-app-rabbitmq
    image: rabbitmq:4.2-management-alpine
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - default

  sonar:
    container_name: sonar
    image: sonarqube:community
    restart: unless-stopped
    ports:
      - "9000:9000"
    networks:
      - default
    volumes:
      - sonar_data:/opt/sonarqube/data

#  blogs-app-spring:
#    container_name: blogs-app-spring
#    build:
#      context: ../../
#    restart: unless-stopped
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://blogs-app-db:5432/${POSTGRES_DB}
#      POSTGRES_USERNAME: ${POSTGRES_USER}
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
#    depends_on:
#      - blogs-app-db
#    networks:
#      - default

volumes:
  postgres_data:
  sonar_data:
  rabbitmq_data:

networks:
  default:
